# 데이터소스는 AbstractContainerTest가 Testcontainers PostgreSQL로 동적 주입한다.
# ddl-auto(validate)와 flyway locations는 application.yml의 공통 설정을 그대로 쓴다 (seed 미포함).
spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true